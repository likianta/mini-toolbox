[tool.poetry]
name = "pyapp-window"
version = "0.4.0a5"
description = ""
authors = ["likianta <likianta@foxmail.com>"]
# readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
argsense = "*"

[tool.poetry.group.webview.dependencies]
# pywebview = { version = "*", platform = "win32" }
# toga = "^0.4.0"
toga = { version = "^0.4.0", platform = "darwin" }
# wxpython = "^4.2.0"
# wxpython = { version = "^4.2.0", markers = "sys_platform != 'darwin'" }
# wxpython = { version = "^4.2.0", platform = "linux" }
# wxpython = { version = "^4.2.0", markers = "sys_platform == 'linux'" }

# explicitly specify version for toga dependency on windows.
pythonnet = { version = "^3.0.0", python = ">=3.8,<3.13", platform = "win32" }  # <- toga (windows)

[tool.poetry.group.dev.dependencies]
pywebview = "*"
wxpython = "*"

[[tool.poetry.source]]
name = "tsinghua"
url = "https://pypi.tuna.tsinghua.edu.cn/simple/"
priority = "default"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project]
version = "0.1.0"
